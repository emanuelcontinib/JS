let drumms = ["tama", "mapex", "dw", "sonor","rmv"];
let includes= (drumms.includes("dw")); //procura o elemento no array e retorna um true ou o que for pedido;

if (includes = true){
    console.log("tem sim");
    
}
